@model string

@(Html
    .Kendo()
    .ComboBoxFor(m => m)
    .Filter(FilterType.Contains)
    .Placeholder("Select contest")
    .DataTextField("Text")
    .DataValueField("Value")
    .DataSource(data =>
    {
        data.ServerFiltering(true)
            .Read(read => read.Action("GetContests", "ExamGroups").Data("filterContests"));
    })
    .HtmlAttributes(new { style = "width:100%" }))